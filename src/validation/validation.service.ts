import { Injectable } from '@nestjs/common';

@Injectable()
export class ValidationService {

    isPalindrome(word: string): boolean {
        return false; // Implémentation temporaire.
      }
      
}
